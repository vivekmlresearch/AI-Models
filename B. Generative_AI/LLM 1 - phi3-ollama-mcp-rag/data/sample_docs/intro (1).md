# MCP-RAG Assistant

This repository demonstrates Retrieval-Augmented Generation (RAG) with:
- Local LLM via Ollama
- Chroma vector store
- A minimal MCP tool server for retrieval

Citations are returned as source + chunk ids.
